# Environment Configuration Template
# This file shows available environment variables
# Copy to .env.local and fill with your values
# IMPORTANT: Never commit .env.local or credentials to Git

# Google API Credentials
# Set GOOGLE_APPLICATION_CREDENTIALS in your shell to point to your service account JSON key
# Example (Linux/macOS):
#   export GOOGLE_APPLICATION_CREDENTIALS=~/.config/service-account-key.json
# Example (Windows PowerShell):
#   $env:GOOGLE_APPLICATION_CREDENTIALS = "C:\Users\YourName\.config\service-account-key.json"
# See docs/START_HERE.md for setup instructions

# Google Sheets Configuration (optional defaults)
# GOOGLE_SHEETS_SPREADSHEET_ID=  # Can be overridden via --sheet-id CLI flag

# Optional Integrations
# SLACK_WEBHOOK_URL=  # For notifications (if supported)

# Optional: Debugging
# BDR_ADAPTER_METRICS=0  # Set to 1 to enable metrics reporting
# DEBUG=false  # Set to true for verbose logging
