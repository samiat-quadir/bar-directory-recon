# Environment Configuration Template
# This file shows available environment variables
# Copy to .env.local and fill with your values
# IMPORTANT: Never commit .env.local or credentials to Git

# Google Sheets API Credentials
# Set GOOGLE_SHEETS_CREDENTIALS_PATH to point to your service account JSON key
# Example (Linux/macOS):
#   export GOOGLE_SHEETS_CREDENTIALS_PATH=~/.config/gcp-key.json
# Example (Windows PowerShell):
#   $env:GOOGLE_SHEETS_CREDENTIALS_PATH = "C:\Users\YourName\.config\gcp-key.json"
# See docs/setup/gsheets.md for full setup instructions

# Google Sheets Configuration
GOOGLE_SHEETS_CREDENTIALS_PATH=  # Path to service account JSON key file
GOOGLE_SHEETS_SPREADSHEET_ID=    # Default Google Sheet ID (can be overridden via --sheet-id CLI flag)

# Optional Integrations
# SLACK_WEBHOOK_URL=  # For notifications (if supported)

# Optional: Debugging
# BDR_ADAPTER_METRICS=0  # Set to 1 to enable metrics reporting
# DEBUG=false  # Set to true for verbose logging
