{
  "required_status_checks": {
    "strict": true,
    "checks": [
      { "context": "audit" },
      { "context": "fast-tests (ubuntu-latest)" },
      { "context": "fast-tests (windows-latest)" },
      { "context": "workflow-guard" }
    ]
  }
}