
[pytest]
addopts = -q -m "not slow and not e2e and not integration"
markers =
	slow: slow tests
	e2e: end-to-end tests
	integration: integration tests
	analytics: analytics tests
	fuzz: fuzz testing with hypothesis
